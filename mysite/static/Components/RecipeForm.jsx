'use strict';

const e = React.createElement

class RecipeForm extends React.Component {
    render() {
        return (
            context.recipe
        );
    }
}

const form = document.querySelector('#RecipeForm');
ReactDOM.render(<RecipeForm  />, form);